# Dependencia de la GUI (tkinter y json son de la libreria estandar)
customtkinter==5.2.2

# Arrastrar y soltar el CSV de frecuencia cardiaca del reloj en la sesion
# guiada (ver heart_rate_import.py). Si no se puede instalar/registrar en
# algun equipo, la pantalla cae sola al boton "Seleccionar archivo".
tkinterdnd2==0.4.2

# Recorte y resumen por etapa de los CSV en vivo (NeuroSky/Emotion/Eye/
# reloj), leidos para la matriz de datos guardados en la pestaña Sesión
# (ver stage_capture.py).
pandas==2.2.2

# tools/NeuroSky/test_neurosky.py (lanzado por neurosky_launcher.py con
# el .venv unificado, ver venv_python() en game_launcher.py) habla con el
# MindWave Mobile por puerto serial/rfcomm -- sin este paquete, "Reparar
# entorno" deja el resto de la app funcionando pero NeuroSky falla con
# "Falta la libreria 'pyserial'" recien al intentar conectarlo.
pyserial==3.5

# El prompt aislado de la Etapa 2 (ver isolated_prompt.py) usa el CLI
# `claude` ya requerido por el resto del proyecto -- no necesita paquetes
# de Python adicionales ni una ANTHROPIC_API_KEY propia.
