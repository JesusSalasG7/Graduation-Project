# Dependencias para tools/emotion_tracker.py
# Recoleccion de datos emocionales - modulo de tesis "Vibe Coding"
#
# tensorflow>=2.19 y sin fijar numpy: las versiones anteriores de TF exigen
# numpy<2.0, lo cual choca con scipy/librosa (Game-06) que ya viven en el
# mismo .venv unificado y requieren numpy>=2.0.

opencv-python==4.10.0.84
deepface==0.0.93
tensorflow==2.21.0
tf-keras==2.21.0
pandas==2.2.2

# Dependencias para tools/eye_tracker.py
# mediapipe>=0.10.16 elimino la API "solutions" (mp.solutions.face_mesh)
# en favor de la nueva Tasks API, por eso se fija 0.10.14: es la ultima
# version con soporte py3.12 que aun trae Face Mesh con iris landmarks
# via la API clasica. matplotlib/attrs/sounddevice son dependencias
# directas de mediapipe.solutions; usa el opencv-python ya instalado
# arriba (no requiere opencv-contrib-python ni jax/jaxlib/sentencepiece
# para Face Mesh).
mediapipe==0.10.14
matplotlib==3.11.1
attrs==26.1.0
sounddevice==0.5.6

# tools/smartwatch/procesar_export.py no requiere dependencias externas
# (solo librería estándar: csv, json, argparse, datetime, pathlib).
