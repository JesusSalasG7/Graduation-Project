# Dependencias para tools/emotion_tracker.py
# Recoleccion de datos emocionales - modulo de tesis "Vibe Coding"
#
# tensorflow>=2.19 y sin fijar numpy: las versiones anteriores de TF exigen
# numpy<2.0, lo cual choca con scipy/librosa (Game-06) que ya viven en el
# mismo .venv unificado y requieren numpy>=2.0.

opencv-python==4.10.0.84
deepface==0.0.93
tensorflow==2.21.0
tf-keras==2.21.0
pandas==2.2.2

# tools/eye_tracker.py NO va aqui: usa mediapipe, que depende de
# opencv-contrib-python. Ese paquete no puede convivir en el mismo
# entorno con opencv-python (dependencia de deepface, arriba): ambos
# instalan archivos en el mismo directorio cv2/ y se pisan entre si,
# dejando cv2 roto (ej. las haarcascades quedan vacias). Ver
# tools/requirements-eyetracker.txt e instalarlo en un venv separado
# (tools/.venv-eyetracker).

# tools/smartwatch/procesar_export.py no requiere dependencias externas
# (solo librería estándar: csv, json, argparse, datetime, pathlib).
